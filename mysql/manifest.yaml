apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql
  namespace: expense
spec:
  storageClassName: "expense-ebs" # Empty string must be explicitly set otherwise default StorageClass will be set
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi

---

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql
  namespace: expense
spec:
  selector:
    matchLabels:
      component: mysql
      project: expense
      tier: db
  serviceName: "mysql"  # This can be any internal service name, ideally use a Headless Service for stable DNS
  replicas: 3
  template:
    metadata:
      labels:
        component: mysql
        project: expense
        tier: db
    spec:
      containers:
      - name: mysql
        image: charan466/mysql:1.0.0
        ports:
        - containerPort: 3306
          name: mysql
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: ExpenseApp@1
        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: mysql-storage
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: "expense-ebs"  # Your existing storage class
      resources:
        requests:
          storage: 4Gi


# apiVersion: apps/v1
# kind: StatefulSet
# metadata:
#   name: mysql
#   namespace: expense
# spec:
#   selector:
#     matchLabels:
#       # has to match .spec.template.metadata.labels
#       component: mysql
#       project: expense
#       tier: db
#   serviceName: "nginx"
#   replicas: 3 # by default is 1
#   template:
#     metadata:
#       labels:
#         # has to match .spec.selector.matchLabels
#         component: mysql
#         project: expense
#         tier: db
#     spec:
#       containers:
#       - name: mysql
#         image: charan466/mysql:1.0.0
#         ports:
#         - containerPort: 3306
#           name: mysql
#         env:
#           - name: MYSQL_ROOT_PASSWORD
#             value: ExpenseApp@1
#         volumeMounts:
#         - name: mysql
#           mountPath: /var/lib/mysql
#       volumes:
#       - name: mysql
#         persistentVolumeClaim:
#           claimName: mysql